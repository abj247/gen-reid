# Shared code

Support code used by both methods. This is not a third method.

```
mem_common.py   Evidence-window loading, frame grids, prompt construction.
analysis/       The mechanism analysis that compares the two methods, and the paper figures.
```

`analysis/README.md` gives the command sequence that reproduces every number and figure reported
for the two methods.
