#!/bin/bash
# Auto-generated individual baseline runner: ID-method 'solider' + tracker 'bytetrack' (all 17 VLMs).
# Equivalent to:  ../run_combo.sh solider bytetrack
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$HERE/../run_combo.sh" "solider" "bytetrack" "$@"
