#!/bin/bash
# Auto-generated individual baseline runner: ID-method 'clipreid' + tracker 'strongsort' (all 17 VLMs).
# Equivalent to:  ../run_combo.sh clipreid strongsort
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$HERE/../run_combo.sh" "clipreid" "strongsort" "$@"
