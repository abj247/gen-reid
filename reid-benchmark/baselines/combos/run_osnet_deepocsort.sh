#!/bin/bash
# Auto-generated individual baseline runner: ID-method 'osnet' + tracker 'deepocsort' (all 17 VLMs).
# Equivalent to:  ../run_combo.sh osnet deepocsort
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$HERE/../run_combo.sh" "osnet" "deepocsort" "$@"
