#!/bin/bash
# Auto-generated individual baseline runner: ID-method 'transreid' + tracker 'botsort' (all 17 VLMs).
# Equivalent to:  ../run_combo.sh transreid botsort
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$HERE/../run_combo.sh" "transreid" "botsort" "$@"
