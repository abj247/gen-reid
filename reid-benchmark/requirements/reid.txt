# Main environment: Qwen2/2.5/3-VL, Ovis2.5, InternVL3, Gemma3, Video-LLaVA,
# VideoChat-Flash (loads here too via a stub flash_attn, see README troubleshooting),
# plus all analysis + plotting + video-property tooling.
# Create with: conda create -n reid python=3.11 -y && pip install -r reid.txt
#
# Verified versions (Python 3.11, CUDA 12.x). Looser pins are fine; these are
# the combinations we actually ran.
torch>=2.4
transformers==4.57.1
tokenizers>=0.21
accelerate>=1.0
bitsandbytes>=0.43
qwen-vl-utils>=0.0.10
timm>=1.0
einops
sentencepiece
decord==0.6.0
av
imageio
imageio-ffmpeg
opencv-python
pillow
numpy<3
# analysis + plotting
matplotlib
seaborn
pandas
scipy
# video-property failure analysis (scene cuts + person detection + CLIP variability)
scenedetect>=0.7
ultralytics>=8.4
