# MA-LMM environment (memory-augmented InstructBLIP, Vicuna-7B backbone).
# MA-LMM is built on Salesforce LAVIS, pinned to old transformers. Setup:
#   conda create -n ma-lmm python=3.10 -y   (use 3.10 NOT 3.9: pip in the 3.9
#                                            conda build is broken on this cluster)
#   git clone https://github.com/boheumd/MA-LMM third_party/MA-LMM
#   cd third_party/MA-LMM && pip install -e .   (installs salesforce-lavis)
#   pip install -r ma-lmm.txt                   (applies the version fixes below)
# Run with PYTHONPATH=third_party/MA-LMM. The lvu_models/eval_malmm_*.py wrappers
# point the Vicuna path at lmsys/vicuna-7b-v1.5 (see README troubleshooting).
torch>=2.1
transformers==4.33.3
tokenizers<0.15
timm==0.4.12
decord==0.6.0
numpy==1.26.4
# LAVIS pins opencv 4.5.5.64 which has a numpy ABI clash on modern numpy; override:
opencv-python-headless>=4.8
sentencepiece
einops
pandas
scipy
imageio
