# VideoChat-Flash environment (OpenGVLab VideoChat-Flash 2B / 7B).
# Its bundled modeling code is pinned to transformers ~4.40 cache APIs, so it
# CANNOT run in the main reid env (transformers 4.57). Create a separate env:
#   conda create -n videochat-flash python=3.10 -y
#   pip install -r videochat-flash.txt
# Then install the stub flash_attn package (see README troubleshooting) so the
# model's import check passes without compiling flash-attn.
torch>=2.2
transformers==4.40.1
tokenizers==0.19.1
accelerate==0.30.1
huggingface_hub==0.23.4
timm==0.9.16
einops
sentencepiece
decord==0.6.0
av
imageio
imageio-ffmpeg
opencv-python
pillow
numpy<3
bitsandbytes
# needed because src/vlm_models.py imports these at module load
matplotlib
pandas
seaborn
scipy
