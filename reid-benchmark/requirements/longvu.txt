# LongVU environment (Vision-CAIR/LongVU_Qwen2_7B).
# LongVU ships as an importable package in its own GitHub repo (no pip release),
# pinned to transformers 4.42.x. Setup:
#   conda create -n longvu python=3.10 -y
#   git clone https://github.com/Vision-CAIR/LongVU third_party/LongVU
#   pip install -r longvu.txt           # installs the pinned deps below
#   # then run with PYTHONPATH=third_party/LongVU (the longvu package is imported from there)
# The lvu_models/eval_longvu_*.py wrappers expect that PYTHONPATH.
torch==2.1.2
transformers==4.42.4
tokenizers==0.19.1
accelerate==0.34.2
huggingface_hub==0.25.1
timm==0.9.16
einops==0.6.1
sentencepiece==0.1.99
decord==0.6.0
numpy==1.26.4
pillow
bitsandbytes==0.41.0
# imported at module load by the wrapper / for plots
matplotlib
pandas
seaborn
scipy
